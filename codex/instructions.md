Legacy compatibility note:

Active Codex guidance now lives in `codex/AGENTS.md` for global behavior, the repository `AGENTS.md` files for project behavior, and `codex/.agents/skills/` for reusable workflows installed into `~/.codex/skills`.

Keep this file only as a reference for older setups that still expect it in the repository.
